syntax = "proto3";

package graphs;

enum ErrorType {
    UNKNOWN_ERROR = 0;
    UNAUTHORIZED = 1;
    USER_CREATION = 2;
};

message ErrorMessage {
    ErrorType type = 1;

    string message = 2;
};
